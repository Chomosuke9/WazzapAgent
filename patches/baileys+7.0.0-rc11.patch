diff --git a/node_modules/baileys/lib/Socket/messages-send.js b/node_modules/baileys/lib/Socket/messages-send.js
index d61e064..99259a5 100644
--- a/node_modules/baileys/lib/Socket/messages-send.js
+++ b/node_modules/baileys/lib/Socket/messages-send.js
@@ -949,6 +949,14 @@ export const makeMessagesSocket = (config) => {
         else if (message.groupInviteMessage) {
             return 'url';
         }
+        else if (message.groupStatusMessageV2) {
+            const innerMsg = message.groupStatusMessageV2.message || {};
+            if (innerMsg.imageMessage) return 'image';
+            if (innerMsg.videoMessage) return innerMsg.videoMessage.gifPlayback ? 'gif' : 'video';
+            if (innerMsg.audioMessage) return innerMsg.audioMessage.ptt ? 'ptt' : 'audio';
+            if (innerMsg.stickerMessage) return 'sticker';
+            return 'text';
+        }
         return '';
     };
     const issuePrivacyTokens = async (jids, timestamp) => {
